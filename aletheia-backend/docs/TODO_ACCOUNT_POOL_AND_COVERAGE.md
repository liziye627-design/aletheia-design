# TODO: 号池与资讯覆盖补齐计划

更新时间：2026-03-04
范围：后端抓取与证据供给（微博/知乎 + 新闻源）

## 目标（本周）
- 在不降低可信度的前提下，提高“正相关证据条数”。
- 避免 `0 命中` 和“热榜顶替证据”。

## 核心指标（验收口径）
- `evidence_items_per_query >= 12`（中文热点问句）
- `entity_hit_rate >= 0.80`（证据中实体命中率）
- `official_or_major_media_ratio >= 0.60`（证据池中高可信来源占比）
- `fallback_hot_ratio == 0`（热榜不进入证据池）

## A. 号池（微博/知乎）
- [x] A1. 代码支持号池轮转、失败熔断与冷却（已完成）
- [ ] A2. 准备微博账号池（3-5个可用 Cookie）
- [ ] A3. 准备知乎账号池（3-5个可用 Cookie）
- [ ] A4. 配置 `.env`：`WEIBO_COOKIES_POOL` / `ZHIHU_COOKIES_POOL`
- [ ] A5. 做一次 50 查询稳定性压测（统计 401/403/429、冷却触发、成功率）
- [ ] A6. 输出号池健康报告（可用账号数、冷却账号数、成功率）

## B. 提升“资讯数量”的可行手段（按优先级）

### P0（先做，1-2天见效）
- [x] B1. 中文主证据链扩容：`rss_pool + 官方源 + weibo + zhihu` 并行（已提供 `run_supplemental_crawl.py`）
- [ ] B2. 对“无 search 能力”官方站改为：列表页/RSS 增量抓取 -> 本地索引检索
- [ ] B3. External Search 固定启用 `always`，并限定白名单域名（只补充，不污染）
- [ ] B4. 对每轮抓取执行实体门禁（未命中实体只进背景池，不进证据池）

### P1（稳态优化，2-4天）
- [ ] B5. 为每个平台增加“预算调度”：主链路高预算，补充链路低预算
- [ ] B6. 增加 query 扩展（别名/同义词）提升召回：如“苏炳添/苏神/短跑名将”
- [ ] B7. 加强正文抽取与去重，减少重复转载占位
- [ ] B8. 增加缓存与 stale 回退，避免网络抖动导致 0 条

### P2（进阶，后续）
- [ ] B9. 接入自建 SearXNG（私有实例）作为可控外部检索补充
- [ ] B10. 引入向量召回（embedding）做二级补召回
- [ ] B11. 建立 query 模板库（事实核查、政策公告、突发事件）

## C. 风险与约束
- 不做验证码绕过与违规采集。
- 号池只用于合法账号与可公开访问内容。
- 社媒结果默认低可信，必须经实体门禁 + 来源分层后入证据池。

## D. 今日执行顺序
- [ ] D1. 补齐微博/知乎号池配置
- [ ] D2. 跑 `source-health-report-zh-core` 新一轮检测
- [ ] D3. 对比改造前后：证据条数、正相关率、高可信占比
- [ ] D4. 输出平台状态表：已稳定 / 待修复 / 下线
